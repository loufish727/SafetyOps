# SafetyOps LFES Control Matrix

Status is based on repository evidence reviewed on 2026-08-03.

| LFES control | SafetyOps application | Status | Evidence or gap |
|---|---|---:|---|
| LFES-SEC-001 | Keep every company record tenant-scoped and enforce membership in PostgreSQL. | PARTIAL | Migration `016` is live and SHA-bound in the hosted ledger; named employee tables report RLS enabled, zero anonymous table grants, and only capability-bound handoff execution for `anon`. No hosted two-company authenticated-role test exists. |
| LFES-SEC-002 | Treat UI permissions as convenience only; RLS, RPCs, and functions authorize writes. | DESIGNED | Narrow inspection/form/onboarding RPCs and SQL policies exist; mocked browser denials cannot prove PostgreSQL or Storage. |
| LFES-SEC-003 / LFES-DEP-003 | Keep tenant material, service keys, and provider secrets out of GitHub Pages. | PARTIAL | The sanitized release uses an Ed25519 v2 attestation over exact `dist` and release-tree file lists, and the verifier rejects Git-tracked sensitive/excluded paths. The private working history must still never be published, and live secret handling is untested. |
| LFES-SEC-005 | Return useful errors without disclosing cross-tenant existence or credentials. | PARTIAL | Configuration, auth, workspace, and mutation errors render in browser tests; selected hosted anonymous Data API, privileged-RPC, signup, and Storage requests failed closed without tenant rows. Authenticated hostile requests remain untested. |
| LFES-DB-001 | Preserve company/location context on operational, program, file, and evidence records. | PARTIAL | Sixteen migrations define compound tenant/location foreign keys, state-aware location bootstrap, immutable inspection/form/employee evidence, and exact-location employee workflows. Migration `016` is applied; exact legacy identity and runtime role/workflow tests remain open. |
| LFES-DB-002 / LFES-DB-003 | Keep RLS enabled and use mutation checks. | PARTIAL | Hosted catalog inspection found RLS on every public table, no reviewed security-definer function without pinned `search_path`, and service-only owner bootstrap grants. Full policy/grant/owner/trigger assertions and authenticated role tests remain absent. |
| LFES-DB-004 / LFES-DB-005 | Preserve relationship integrity and indexes for tenant, location, status, and lineage paths. | DESIGNED | Foreign keys, typed evidence links, checks, hashes, idempotency, and indexes exist. Migration `010` derives signature identity, role, method, intent, time, manifests, and hashes from pinned records rather than trusting browser-authored evidence; no clean replay of the exact final source chain or query-plan proof exists. |
| LFES-DB-006 | Keep migrations ordered, reviewable, and followed by application QA. | PARTIAL | Sixteen ordered migrations exist; `016` passed hosted rollback compilation, application, SHA-bound ledger recording, and named catalog checks. Earlier applied-file checksums and hostile-role runtime tests remain open. |
| LFES-REL-001 / LFES-REL-003 / LFES-REL-005 | Make configuration, signed-out, empty, loading, and workspace-error states explicit. | PARTIAL | The 91-pass browser run covers the prior shell/auth states plus committee, training, employee tablet handoff, dashboard status, signature, and PDF quarantine contracts; real persistence, reconnect, scanner behavior, and provider delivery are untested. |
| LFES-SCL-001 / LFES-SCL-002 | Bound large reads and keep location/program history scalable. | PARTIAL | Workspace collections have explicit 30–1,000-row caps; pagination, truncation indicators, and detail-on-demand remain missing. |
| LFES-MNT-001 | Reduce responsibility concentration only through controlled extraction. | PARTIAL | `app.js` still owns auth, queries, mapping, rendering, events, and mutations; the passing suite is a checkpoint, not a reason for unplanned refactoring. |
| LFES-MNT-002 / LFES-REV-001 | Preserve engineering memory and clear reviewer entry points. | PROVEN | LFES, architecture, QA, risk, release, and Supabase documents provide current review entry points. |
| LFES-TRACE-001 / LFES-REV-004 | State exactly what each test proved and did not prove. | PROVEN | `PROOF_MODEL.md` and `QA_LOG.md` separate browser, signed-artifact, database, Edge, hosted, and regulatory evidence. |
| LFES-OBS-001 | Preserve audit events and visible operational failures. | PARTIAL | Atomic provisioning produced six system-attributed audit events with NULL human actor, and visible errors exist in source; hosted log review, alerting, retention, and export are unproven. |
| LFES-EVOL-001 / LFES-DEP-004 | Require checkpoints, rollback, and data-preserving correction plans for high-risk changes. | UNPROVEN | Frontend source is recoverable, but database backup/restore, migration rollback, and forward-correction exercises are not recorded. |
| LFES-DEP-001 / LFES-DEP-002 | Make GitHub Pages deployment and public configuration understandable. | PROVEN | The Pages workflow builds/tests, verifies the signed v2 exact-tree attestation, and deploys only `dist`; persistent auth remains disabled on the shared origin. |
| LFES-DEP-005 | Verify the exact public artifact and hosted URL after deployment. | PARTIAL | Exact sanitized release-tree and deployable-artifact identity is CI-verified, Pages deployment passed, and the cache-busted live desktop sign-in shell rendered. Mobile live, authenticated tenant, and rollback proof remain absent. |
| LFES-MOB-001 / LFES-MOB-002 / LFES-MOB-003 | Verify responsive, weak-network, and auth behavior on mobile. | PARTIAL | Desktop and Pixel 7 projects passed; real-device accessibility, weak-network, reconnect, and persistent-session behavior remain unproven. |
| LFES-VER-001 / LFES-VER-002 | Match selectors to responsive UI and distinguish harness health from product behavior. | PROVEN | Latest full suite: 91 passed, 7 conditional/project skips, 0 failed. The fake Supabase harness proves browser contracts only. |
